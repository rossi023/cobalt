---
title: "Version 10.0"
published_date: 2025-11-13
description: "Major update with stability improvements and performance fixes."
tags:
  - release
  - update
layout: default.liquid
---

### 🚀 Highlights

- Improved build performance on Cloudflare and GitHub Actions.
- Fixed YAML frontmatter parsing issues.
- Added better error handling and deployment logs.

### 🛠 Technical Changes

- Updated to use `cobalt-bin` instead of `cobalt` crate.
- Adjusted workflow for `gh-pages` static deployment.
- Cleaned redundant `package.json` checks.

---

> ✅ Tip: When writing YAML frontmatter for Cobalt:
> - Always use **double quotes** `"` for strings that contain punctuation; avoid smart/curly quotes.  
> - Dates: use ISO format `YYYY-MM-DD` or `YYYY-MM-DDTHH:MM:SSZ`.  
> - Field names: use Cobalt-supported names like `published_date`, `title`, `description`, `tags`, `layout`.
